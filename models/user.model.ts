export type EmailingContact = {
  email: string;
  newsletter: boolean;
  productUpdates: boolean;
  coursePreview: boolean;
};

export type EmailingStatuses = {
  newsletter: boolean;
  productUpdates: boolean;
  coursePreview: boolean;
};
